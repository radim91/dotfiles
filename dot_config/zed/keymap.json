// Zed keymap
//
// For information on binding keys, see the Zed
// documentation: https://zed.dev/docs/key-bindings
//
// To see the default key bindings run `zed: open default keymap`
// from the command palette.
[
  {
    "context": "Workspace",
    "bindings": {
      // "shift shift": "file_finder::Toggle"
    },
  },
  {
    "context": "Editor && vim_mode == insert",
    "bindings": {
      // "j k": "vim::NormalBefore"
    },
  },
  {
    "context": "((vim_mode == helix_normal || vim_mode == helix_select) && !menu)",
    "bindings": {
      "ctrl-s": "pane::SplitRight"
    }
  },
  {
    "context": "((VimControl && !menu) || (!Editor && !Terminal))",
    "bindings": {
      "ctrl-s": "pane::SplitVertical"
    }
  },
  {
    "context": "((VimControl && !menu) || (!Editor && !Terminal))",
    "bindings": {
      "ctrl-shift-right": "workspace::ActivateNextPane"
    }
  },
  {
    "context": "((VimControl && !menu) || (!Editor && !Terminal))",
    "bindings": {
      "ctrl-shift-left": "workspace::ActivatePreviousPane"
    }
  },
  {
    "context": "Editor",
    "bindings": {
      "ctrl-shift-up": "editor::MoveLineUp"
    }
  },
  {
    "context": "Editor",
    "bindings": {
      "ctrl-shift-down": "editor::MoveLineDown"
    }
  },
  {
    "context": "Workspace",
    "bindings": {
      "ctrl-?": "workspace::ToggleRightDock"
    }
  },
  {
    "context": "((vim_mode == helix_normal || vim_mode == helix_select) && !menu)",
    "bindings": {
      "space f f": "file_finder::Toggle"
    }
  },
  {
    "context": "Pane",
    "bindings": {
      "space f f": "project_search::ToggleFocus"
    }
  },
  {
    "context": "ProjectSearchBar",
    "bindings": {
      "space f f": "search::FocusSearch"
    }
  },
  {
    "context": "ProjectPanel",
    "bindings": {
      "ctrl-shift-f": "project_panel::NewSearchInDirectory"
    }
  },
  {
    "bindings": {
      "ctrl-alt-q": "zed::Quit"
    }
  },
  {
    "context": "TabSwitcher",
    "bindings": {
      "alt-q": "tab_switcher::CloseSelectedItem"
    }
  },
  {
    "context": "((VimControl && !menu) || (!Editor && !Terminal))",
    "bindings": {
      "alt-q": "pane::CloseActiveItem"
    }
  },
  {
    "context": "(VimControl && !menu)",
    "bindings": {
      "alt-left": "pane::GoBack"
    }
  },
  {
    "context": "(VimControl && !menu)",
    "bindings": {
      "alt-right": "pane::GoForward"
    }
  },
  {
    "context": "(Editor && mode == full)",
    "bindings": {
      "space f f": "file_finder::Toggle"
    }
  },
  {
    "context": "((VimControl && !menu) || (!Editor && !Terminal))",
    "bindings": {
      "alt-shift-right": "vim::ResizePaneLeft"
    }
  },
  {
    "context": "((VimControl && !menu) || (!Editor && !Terminal))",
    "bindings": {
      "alt-shift-left": "vim::ResizePaneRight"
    }
  },
  {
    "context": "((vim_mode == helix_normal || vim_mode == helix_select) && !menu)",
    "bindings": {
      "space a": "editor::ToggleCodeActions"
    }
  },
  {
    "context": "(VimControl && !menu)",
    "bindings": {
      "space a c": "editor::ToggleCodeActions"
    }
  },
  {
    "context": "Workspace",
    "bindings": {
      "space f b": "tab_switcher::Toggle"
    }
  },
  {
    "context": "vim_mode == insert",
    "bindings": {
      "alt-,": "assistant::InlineAssist"
    }
  },
  {
    "context": "(Editor && mode == full)",
    "bindings": {
      "alt-,": "assistant::InlineAssist"
    }
  },
  {
    "context": "((vim_mode == helix_normal || vim_mode == helix_select) && !menu)",
    "bindings": {
      "space f c": "outline::Toggle"
    }
  },
  {
    "context": "(VimControl && !menu)",
    "bindings": {
      "space f c": "outline::Toggle"
    }
  }
]
